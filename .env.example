# ═══════════════════════════════════════════════════════════════════════════════
# COMFYUI + VAST.AI - CONFIGURAÇÕES
# ═══════════════════════════════════════════════════════════════════════════════
# Copie estas variáveis para o painel do Coolify em: Environment Variables
# Ou renomeie este arquivo para .env para uso local
# ═══════════════════════════════════════════════════════════════════════════════


# ┌─────────────────────────────────────────────────────────────────────────────┐
# │                           COMFYUI - BÁSICO                                  │
# └─────────────────────────────────────────────────────────────────────────────┘

# Porta do ComfyUI (default: 8188)
COMFYUI_PORT=8188

# Imagem Docker (não altere a menos que saiba o que está fazendo)
COMFYUI_IMAGE=ghcr.io/yanwenkun/comfyui-docker:cpu

# Argumentos CLI do ComfyUI
# Para CPU (default): --cpu --listen --preview-method auto
# Se tiver GPU local: --listen --preview-method auto
COMFYUI_CLI_ARGS=--cpu --listen --preview-method auto


# ┌─────────────────────────────────────────────────────────────────────────────┐
# │                           VAST.AI - GPU SOB DEMANDA                         │
# └─────────────────────────────────────────────────────────────────────────────┘

# Sua API Key do Vast.ai
# Obtenha em: https://vast.ai/console/account
VASTAI_API_KEY=

# Preço máximo por hora em USD (default: $0.50)
# Exemplo: 0.30 = máximo $0.30/hora
VASTAI_MAX_PRICE=0.50

# GPUs preferidas (separadas por vírgula)
# Opções: RTX 3090, RTX 4090, RTX A5000, RTX A6000, A100
VASTAI_PREFERRED_GPUS=RTX 3090,RTX 4090,RTX A5000


# ┌─────────────────────────────────────────────────────────────────────────────┐
# │                           S3 STORAGE (OPCIONAL)                             │
# └─────────────────────────────────────────────────────────────────────────────┘
# Configure para salvar resultados automaticamente na nuvem
# Compatível com: AWS S3, MinIO, Cloudflare R2, Backblaze B2

S3_ACCESS_KEY_ID=
S3_SECRET_ACCESS_KEY=
S3_BUCKET_NAME=
S3_ENDPOINT_URL=
S3_REGION=auto


# ┌─────────────────────────────────────────────────────────────────────────────┐
# │                           RECURSOS DO CONTAINER                             │
# └─────────────────────────────────────────────────────────────────────────────┘

# Limite de memória RAM (default: 2G)
# Aumente se tiver mais RAM disponível
MEMORY_LIMIT=2G

# Reserva mínima de memória (default: 512M)
MEMORY_RESERVATION=512M
